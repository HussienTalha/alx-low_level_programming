#include <stdio.h>
#include <string.h>
/**
 * main - print program name
 * @argc: command line argument counter
 * @argv: array of pointers to the strings passed to main
 * Return: always return 0
 *
 *
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc - argc]);
return (0);
}
