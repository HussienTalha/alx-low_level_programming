#include "main.h"
/**
 * _isdigit - check if charchter is 0-9 digit
 * @c: charcter to be checked
 * Return: 1 if digit 0 otherwise
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 55)
return (1);
else
return (0);
return (0);
}
