#include "main.h"
/**
 * _isupper - check if uppercase
 * @c: ascii value of charchter
 * Return: return 1 if upper case 0 if lower case
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
return (0);
}
